import { RouteRecordRaw } from 'vue-router';

const <%= name %>Index = () =>
  import(/* webpackChunkName: '<%= type %>' */ '@/pages/<%= type %>/views/<%= lowername %>/index.vue');

const routes: RouteRecordRaw[] = [
  {
    path: '<%= lowername %>',
    name: '<%= name %>Index',
    component: <%= name %>Index,
    children: [],
  },
];

export default routes;
